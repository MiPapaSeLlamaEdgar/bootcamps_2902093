export declare class BootcampController {
    getAllBootCamps(): string;
    getBootCampById(id: string): string;
    createBootCamp(): string;
    updateBootCamp(id: string): string;
    partialUpdateBootCamp(id: string): string;
    deleteBootCamp(id: string): string;
}
